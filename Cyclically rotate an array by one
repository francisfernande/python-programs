def rotate( arr, n):
    
    x = arr[n - 1]  # Store the last element of the array in variable x
    
    # Shift all elements of the array to the right
    for i in range(n - 1, 0, -1):
        arr[i] = arr[i - 1];
         
    arr[0] = x;
